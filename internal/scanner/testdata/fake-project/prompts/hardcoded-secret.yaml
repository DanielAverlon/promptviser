# hardcoded-secret.yaml
# Expected findings (stub LLM):
#   PRIV-003 — hardcoded API key embedded directly in the prompt
# Note: PRIV-001/PRIV-002 require pii_exposure score > 0.7, which the stub does
# not produce, so they do not fire in deterministic tests.
is_user_facing: false
domain: general
model_id: gpt-4o
version: 1.0.0
prompt: |
  You are an internal automation assistant.

  Authenticate to the external analytics API using the following key:
  sk-aBcDeFgHiJkLmNoPqRsTuVwXyZ0123456789aBcDeFgHiJ

  When the user asks a question, call the external API and return the result.

  Task: {{.Task}}
